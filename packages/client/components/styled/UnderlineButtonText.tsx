import styled from "styled-components";

const UnderlineButtonText = styled.a`
  font-family: "Avenir", sans-serif;
  font-weight: 500;
  font-size: 26px;
  line-height: 30px;
  text-decoration: underline;
`;

export default UnderlineButtonText;
